# Printers

This folder contains the printing function implementations across allocators. I also have the gnuplot printing function here for convenience. These functions are helpful for demonstrating concepts about heap allocators or debugging allocators while implmeneting them.

